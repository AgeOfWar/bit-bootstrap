type Box(T) = [ value: T ]

fun sumBox<T: Integer>(a: Box(T), b: T): Box(T) {
    return [ value: a.value + b as T ]
}

fun main() {
    x: Box(Integer) = [ value: 32 ]
    y = sumBox(x, 10)
    print(y)
}
