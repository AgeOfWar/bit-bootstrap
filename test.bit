type Box(T) = [ value: T ]

fun cloneBox<T: Integer>(a: Box(T), b: T): Box(T) {
    return [ value: a.value + b as T ]
}

fun main() {
    x = [ value: 32 ]
    y = cloneBox<Integer>(x, 10)
    print(y)
}
